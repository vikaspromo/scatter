# Supabase Configuration
SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Gmail OAuth Credentials (for GitHub Actions)
# These should be set as GitHub Secrets, not in local .env
# GMAIL_CREDENTIALS=<contents of credentials.json>
# GMAIL_TOKEN=<contents of token.json>

# For local development:
# 1. Copy this file to .env
# 2. Fill in your Supabase values
# 3. Place credentials.json and token.json in the email-parser directory
# 4. The script will automatically use local files instead of environment variables
